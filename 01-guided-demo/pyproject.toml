[project]
name = "guided-demo"
version = "0.1.0"
description = "Guided Demo — Company Research Briefing Agent"
requires-python = ">=3.11"
dependencies = [
    "claude-agent-sdk>=0.1.41",
    "python-dotenv>=1.0.0",
]

[dependency-groups]
dev = []
